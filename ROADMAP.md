# 次回の設計案（課題と改善計画）

CHANGELOG が「変わってきた道筋」、このファイルは「これからの計画」。
実装が済んだ項目はここから消し、CHANGELOG へ1行移す。

## いま見えている課題

| # | 課題 | 影響 |
|---|---|---|
| 1 | 検索がファイル名にしか効かない | 「中身は覚えているが名前を忘れた」文書に辿り着けない |
| 2 | ブックマークがブラウザ内保存 | ブラウザのデータ消去で消える。複数PCで共有できない |
| 3 | PlantUML は事前生成した SVG が無いと図にならない | 大半の .puml がソース表示止まり |
| 4 | 開こうとしたファイルが無いとき「見つかりません」で終わる | リネーム直後などに行き止まりになる |
| 5 | 全体スキャンが約30秒かかる（毎回全走査） | 起動直後の色つけ・新規反映が待たされる |
| 7 | AIが直近で作った・変えたファイルを一覧する手段がない | 変更の把握がツリーの色だけに頼っている |

## 次の設計案（優先順）

### 2. 最近更新ビュー

- 「今日・昨日・今週」に変更されたファイルを新しい順で一覧する切り替え表示
- ツリーのスキャン結果に更新日時を持たせ、メニューをリスト表示に切り替える

### 3. 全文検索

- ファイル名検索に加えて、テキスト系ファイルの中身を対象にした検索モードを足す
- 読み取り専用の検索APIを1つ追加（対象拡張子・サイズ上限・件数上限で暴走を防ぐ）

### 4. ブックマークのファイル保存化

- 保存先をブラウザ内からワークスペース内の設定ファイルへ移す
- 保存APIを1つ追加（許可フォルダ内の専用ファイル1つだけを書けるようにする）

### 5. PlantUML のその場描画

- ローカルの plantuml.jar で開いた時に SVG を生成し、結果をキャッシュする
- 外部サーバーには送らない（ローカル完結）

### 6. 見つからないファイルの候補表示

- 開けなかったとき、同じフォルダ内の似た名前を候補として提示する

### 7. スキャンの増分化

- 全走査をやめ、変更のあったフォルダだけ調べ直す方式にして数秒に短縮する

## 検討したが今はやらないもの

| 機能 | 見送りの理由 |
|---|---|
| 2画面分割 | 別ウィンドウ切り離しである程度代替できる。複雑さに見合う頻度か様子見 |
| バックリンク | 文書間リンクの利用がまだ少ない。全文検索で代替できる場面が多い |
| 新規ファイル作成・リネーム | 誤操作防止を優先し、編集は既存ファイルの上書きに限定する方針を維持 |
