# 変更履歴

READMEは「今の姿」、このファイルは「変わってきた道筋」を書く。新しい日付を上に足す。
行頭のタグで種類が分かる：**【機能】**=新しくできるようになった／**【修正】**=不具合を直した／**【変更】**=見た目・構成・運用の変更。

## 2026-09-22
- 【機能】マークダウンの見出しに id を付けるようにした。文書の中に書いた目次のリンク（`[見出し](#見出し)`）が、そのまま飛ぶようになる。id の作り方は GitHub と同じ（小文字にし、記号を落とし、空白を `-` にする。同じ見出しが2回出たら `-1` `-2` を足す）
- 【修正】本文の枠（iframe）の中で `#` へのリンクを押したとき、その見出しまで自分でスクロールするようにした。手書きの目次で大文字小文字がずれている場合（`#基本的なSEO対策` など）も拾う

## 2026-09-21
- 【機能】ツリーとブックマーク欄で、同じ場所を指す行どうしが対応して光るようにした（片方にマウスを乗せると、同じパスの行がもう片方で黄色く光る。フォルダ・ファイルとも。どちらの欄からでも効く）
- 【変更】ブックマーク欄のフォルダの行から色を外した（黄色い帯をやめ、▼📁 と太字だけのただの行にした）。色が付くのはマウスを乗せた時と、対応する行が光る時だけ
- 【修正】ブックマーク欄で、一番上の段のフォルダにだけ枝線（横の短い線）が付かず、同じフォルダの行なのに線の有無がばらついて見えていた問題を修正（一番上の段も `.bm-children` で包むようにした）
- 【変更】同日に一度入れた「フォルダごとに8色で色分け」は元に戻した（色が多すぎたため）。ツリーのフォルダの帯は灰色の従来どおり

## 2026-09-20
- 【変更】ブックマーク欄の行の形を、下のツリー（エクスプローラ）とまったく同じにした（フォルダは浮いた帯・ファイルはただの行・縦のガイド線と横の枝線）。色だけ★の黄色に寄せて、ツリーと見分けられるようにした
- 【機能】ツリーの右クリックで小さなメニューが出るようにした（ファイルの行は「ブックマークに登録／から外す」、フォルダの行はそれに加えて「ここにフォルダを作る」）。外側クリックとEscで閉じる
- 【機能】画面からフォルダを作れるようにした（mkdir.php／Flask版も同じ窓口）。作業フォルダの中だけ・1階層だけ・同じ名前があれば断る・削除はしない。作ったフォルダはツリーに出て開いた状態になる
- 【変更】ブックマーク切替の確認ダイアログは廃止（右クリックでメニューが1枚挟まるため、間違って押しただけでは何も起きない）

## 2026-09-19
- 【機能】PDFをビューアの枠の中に簡易表示できるようにした（pdf.js）：Chromeの「PDFをダウンロードする」設定がオンだと内蔵表示が使えずAdobe等に飛んでいたため。内蔵表示が使える設定ではこれまでどおり内蔵表示（検索・印刷可）。日本語の文字対応表（CMap）も指定
- 【修正】画像（.png .jpg .jpeg .gif .webp .bmp .ico）がツリーで灰色になり開けなかった問題を修正。開くと枠の幅に収めて表示する
- 【機能】ブックマーク済みの行は、行頭の c（パスコピー）が黄色になるようにした（★印のような要素を足すと段付きがずれるため、既にある c の色だけ変える。上部★ボタンの点灯色と同じ）
- 【変更】ツリーの右クリックでのブックマーク切替に確認ダイアログを付けた（「登録しますか」「外しますか」＋名前と場所。キャンセルなら何も変えない。意図せず消したり付けたりしないため）
- 【機能】本文の上で右クリックすると、クリップボードの文字をパス欄に入れてEnterと同じ動きにするようにした（「貼る→Enter」の2手を1回に。パスでない文字はファイル名検索。編集中の欄と目次は対象外。初回だけブラウザのクリップボード許可が出る）
- 【機能】ツリーの行を右クリックすると、ブックマークの登録／解除が切り替わるようにした（ファイル・フォルダとも。結果は上のメッセージ欄に2秒表示）
- 【機能】ブックマーク欄の階層行（📁）を押すと、ツリーでそのフォルダを展開して一番上へ出すようにした。ブックマークしたフォルダ自体の行も同じ動き
- 【修正】本文をクリックした後は F1（目次）と Ctrl+B（メニュー）が効かなかった問題を修正：キー入力が本文の枠（iframe）の中に入って外側に届いていなかった。枠の中で押されたキーを外側へ転送するようにした
- 【変更】目次ペインを本文の左に並べる形にした：本文に重ねて右に出していたため表や図が隠れていた。目次を出すと本文は残りの幅に収まる
- 【機能】F1キーで目次の表示／非表示を切り替えられるようにした（ボタンと同じ動き。目次の無いファイルでは何もしない）
- 【変更】ブックマーク一覧をフォルダの入れ子（階層）表示にした：同じフォルダのものが親フォルダの下にまとまる。平らな2行並びをやめた
- 【変更】ファイル名の帯を2段にした：1段目がファイル名、2段目が場所。横並びだと長いパスが切れていた

## 2026-09-12
- 【変更】ツリーの「まとまり順」ソート機能を撤去：切替ボタンの意味が分かりづらく、エクスプローラーと同じ名前順が一番迷わないため（2026-08-30追加分の取り下げ）
- 【変更】ツリー行の★印（ブックマーク済みの印）も廃止：±と同じく、行に印が増えると段付きがずれて階層が読みづらくなるため。ブックマーク機能自体（上部★ボタン・一覧）はそのまま
- 【変更】ツリーの差分（±）ボタンを廃止：行に印が増えると階層が見づらくなるため。gitの色分けは従来どおりで、差分は上部の「差分」ボタンで見る
- 【機能】「畳む: 1 2 3」ボタンを追加：ツリーを深く展開して長くなったとき、指定の階層より深い開きを一括で閉じる（1=全部・2=第2階層から下・3=第3階層から下。畳む専用で浅い側は触らない）

## 2026-09-05
- 【機能】ツリーの再スキャンを増分方式にした：フォルダごとの一覧を更新時刻つき台帳に覚え、変わっていないフォルダは読み直さない。全走査33〜37秒 → PHP版 約5秒・Flask版 約0.3秒（初回だけ台帳作成で1分程度）
- 【修正】「更新中…」のさなかにパス入力で開いた場合も、スキャン終了後にもう1周走らせて必ずツリーに出すようにした（実行中は依頼が捨てられていた）
- 【修正】パス入力で開いたファイルがツリーに未反映（作りたて）のとき、再スキャン後にアクティブにならず一番上にも来ない問題を修正（スクロール位置の復元が展開スクロールを上書きしていた）
- 【修正】下モードのブックマーク一覧で、名前が長い行のパスと×印がずれる問題を修正（名前列を280pxで固定し、長い名前は「…」で切る。パスの頭が全行で縦に揃う）
- 【変更】アイコンの背景を濃い青から黄色に変更（白い鳥は黄色地で見えるよう輪郭を濃い青に）

## 2026-09-02
- 【機能】本文検索の専用欄を追加：ファイル名検索と入り口を分離。左メニューの専用欄にテキストを入れてEnterで、md/htmlの中身にヒットしたファイル一覧（名前・場所・ヒット行）が出て、クリックで開ける
- 【機能】「再表示」ボタンを追加：開いているファイルだけ読み直す。裏で書き換わった内容を、ページ全体を再読込せずに反映できる（F5と違いツリーの開閉・スクロール位置・表示中ファイルが消えない）。編集中は確認してから読み直す
- 【変更】READMEを全面的に読みやすく再構成：スクリーンショット（架空データ）を追加し、クイックスタート（Flask 5行）を先頭へ。機能一覧を最新化し、仕組み図をFlask/PHP両対応に描き直した
- 【変更】ROADMAP.md をリポジトリから外した（私的な作業メモのため。今後の計画はローカルの設計書フォルダで管理）

## 2026-08-30
- 【機能】Flask版サーバー（server.py）を同梱：Python + Flask だけで全機能が動く（ツリー・配信・保存・git差分/色つけ・全文検索・PlantUML）。設定は api/config.example.json をコピーして作る
- 【機能】全文検索を追加：検索欄の候補の最後に「🔎 本文を検索」が出て、Markdown/HTMLの中身から探せる（約5,900ファイルを0.3秒。初回だけ索引作成に数分）。索引は一時フォルダに置き、機密フォルダ（config の ftsExcludeFolders）は絶対に含めない
- 【機能】左の絞り込み欄からも本文検索できるようにした（入力すると欄の下に「🔎 本文を検索」ボタンが出る。名前で0件でも中身から探せる）
- 【機能】PlantUML(.puml) のその場描画を追加：ローカルの plantuml.jar でSVG化して表示（外部送信なし。jarが無ければ従来どおりソース表示）
- 【機能】ツリーの「まとまり順」ソートを追加：ローカル設定に地図ファイル（フォルダ→意味のまとまり対応表）を書くと「並び:名前／意味」の切替ボタンが出て、意味的に近いフォルダが隣に並ぶ。フォルダの実体には触らない。設定が無い環境では何も変わらない
- 【変更】READMEのセットアップを「方法A: Flask（推奨）／方法B: XAMPP」の2本立てに変更。Flaskは clone→copy→pip install flask→python server.py の4手で導入できる

## 2026-08-29
- 【機能】目次ペインを追加：開いたMarkdown/HTMLの見出し一覧を右側に表示、クリックで移動（「目次」ボタンで隠せる）
- 【機能】フォルダも履歴に残るようにした（📁アイコン付き。選ぶとツリー展開）
- 【機能】ビューア上部に、開いているファイル名を大きく常時表示する帯を追加（どの拡張子でも。場所は作業フォルダ以降を添える）
- 【機能】パス入力で開いた項目を、ツリー表示領域の一番上（ブックマーク欄の直下）へ自動スクロールするようにした
- 【修正】ブックマーク欄の高さをつまみのドラッグで自由に変えられるようにした（上限の頭打ちと、中身の量までしか広がらない問題を修正）
- 【変更】ツリーで .md=📝 と .html=🌐 のアイコンを付けて区別できるようにした
- 【変更】プレビュー内のリンクを振り分け：外部URLは新しいタブ、ファイルへの相対リンクはビューア自身で開くようにした（Markdown・HTMLとも）
- 【変更】ROADMAP.md を追加（課題と次の設計案。済んだらCHANGELOGへ移す運用）

## 2026-08-26
- 【変更】ブックマーク一覧を2行表示（名前／作業フォルダ以降のパス）にし、フォルダ名順で並べた
- 【修正】PlantUMLのSVGが未生成のとき、真っ白ではなくソースを表示するようにした

## 2026-08-23〜25
- 【機能】ブックマーク機能を追加（★で登録、上部に一覧、ツリーにも★印）
- 【変更】ファビコンを白い小鳥＋濃い青地に変更
- 【変更】別環境で見た目を再現するためのスタイル仕様書を追加

## 2026-08-22
- 【機能】メニューの置き場所を3モード化（左／下／別ウィンドウへ切り離し）
- 【機能】ソースコードとMarkdownのコードブロックに色付け（highlight.js）
- 【変更】フォルダの行を「浮いた帯」にして、ファイルと一目で区別できるようにした

## 2026-08-10
- 【機能】git連携を追加：ツリーの色つけ（変更=オレンジ・新規=緑、VS Code風）、±ボタンと差分ボタンによる色つき差分表示

## 2026-08-09（初公開）
- 【変更】公開リポジトリとして整理して初公開。個人情報は設定ファイル（git管理外）へ分離
- 主な機能：作業フォルダ全体のツリー、即時検索、Markdown+Mermaid描画、その場で編集、5分キャッシュ＋自動再スキャン
